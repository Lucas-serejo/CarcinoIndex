[pytest]
markers =
    postgresql: requires TEST_DATABASE_URL and a real PostgreSQL server
    sam2_integration: requires the official checkpoint and a CUDA GPU
    sam2_api_integration: requires the official checkpoint, API dependencies, and a CUDA GPU
